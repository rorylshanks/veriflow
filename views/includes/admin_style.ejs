<style>
    body {
      margin: 0;
      font-family: Verdana, sans-serif;
      display: flex;
      height: 100vh;
      box-sizing: border-box;
      background-color: #607da6;
      padding: 40px;
    }
    .sidebar {
      margin-right: 40px;
      padding: 20px;
      width: 250px;
      box-sizing: border-box;
      background-color: #fff;
      box-shadow: 0 0 16px rgba(17, 17, 17, 0.2);
      border-radius: 4px;
    }
    .sidebar a {
      text-align: center;
      text-decoration: none;
      color: #111;
      display: block;
      padding: 20px 0;
      border-radius: 4px;
      text-shadow: 0 0 1px rgba(17, 17, 17, .5);
    }
    .sidebar a:hover {
      background-color: #ddd;
    }
    .content {
      flex-grow: 1;
      padding: 20px;
      box-sizing: border-box;
      background-color: #fff;
      box-shadow: 0 0 16px rgba(17, 17, 17, 0.2);
      border-radius: 4px;
      height: 100%;
      overflow-y: scroll;
    }
    .card {
      background-color: #fff;
      box-shadow: 0 0 5px rgba(17, 17, 17, 0.2);
      padding: 20px;
      border-radius: 4px;
    }
    .card h5 {
        font-size: 1em;
    }
    .card table {
      max-height: 100%;
      border-collapse: collapse;
      font-size: small;
      
    }
    .card th, .card td {
      text-align: left;
      padding: 8px;
      border-bottom: 1px solid #ddd;
    }
    .card th {
      background-color: #f8f8f8;
    }
    .logo {
        padding: 10px;
        margin-bottom: 20px;
    }
    .logo img {
        width: 100%;
        margin: auto;
        display: block;
    }
  </style>